context("test-interpolate.R")

test_that("Test interpolation", {
  # NA_pos <- sample(seq_len(NROW(USAccDeaths)), 10)
  # USAccDeaths$value[NA_pos] <- NA 
  # mbl <- USAccDeaths %>%
  #   fable::LM(value ~ trend() + season())
  # interpolated <- interpolate(mbl)
  # expect_true(all(!is.na(interpolated$value)))
})
